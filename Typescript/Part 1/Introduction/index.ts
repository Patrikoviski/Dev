// Typescript
// npm install -g typescript
// tsc --init

// Plugin: Code Runner
// npm i -g ts-node
// ctrl + alt + n

console.log("npm install -g typescript")

const order = (order: any) => {
    return `your order has been placed ${order}`
}

console.log(order(1232))